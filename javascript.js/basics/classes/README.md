Class Exercise

Run the example with Node:

```
node exercise.js
```

This file demonstrates:

- Defining classes and constructors
- Inheritance with `extends` and `super`
- Getters and setters
- Static methods
- Overriding methods and prototype extension

Open `exercise.js` and modify constructors or methods to experiment.
